문자열 find함수

조합 from itertools import combinations, permutations
combinations(nums, 2)

최대 공약수
def gcd(a, b):
    for i in range(min(a, b), 0, -1):
        if a % i == 0 and b % i == 0:
            return i

str.isdigit()
문자열이 '숫자'로만 이루어져있는지 확인하는 함수 입니다

여러 자리수 숫자문자를 숫자로
int(c[:-1])
